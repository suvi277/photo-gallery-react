import { ROOT_URL, API_ROOT } from './constants';

export const API_CONSTANT = {
    'auth': `${ROOT_URL}/oauth/token`,
    "photos": `${API_ROOT}/photos/`,
    "searchPhotos": `${API_ROOT}/search/photos/`
}
